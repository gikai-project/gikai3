import streamlit as st
from openai import OpenAI
import json
import plotly.graph_objects as go

# ======================================================
# Streamlit 基本設定
# ======================================================
st.set_page_config(
    page_title="一般質問 採点AIシステム（300点モデル）",
    layout="wide"
)

# ======================================================
# OpenAI API Key
# ======================================================
if "OPENAI_API_KEY" not in st.secrets:
    st.error("OpenAI API Key が設定されていません（Secrets）")
    st.stop()

API_KEY = st.secrets["OPENAI_API_KEY"]
MAX_CALLS = int(st.secrets.get("MAX_CALLS", 100))
client = OpenAI(api_key=API_KEY)

# ======================================================
# API使用回数管理
# ======================================================
if "api_calls" not in st.session_state:
    st.session_state.api_calls = 0

def check_api_limit(calls=1):
    if st.session_state.api_calls + calls > MAX_CALLS:
        st.error(f"⚠ API利用上限に達します（上限 {MAX_CALLS} 回）")
        st.stop()

# ======================================================
# 評価項目
# ======================================================
ITEM_NAMES = {
    "1": "テーマ設定の妥当性",
    "2": "目的の明確性",
    "3": "論理構成の明確性",
    "4": "根拠・エビデンスの妥当性",
    "5": "質問の具体性",
    "6": "政策提案の実現可能性",
    "7": "行政答弁を引き出す質問力",
    "8": "議会の役割・法的理解",
    "9": "住民視点・説明責任の明瞭性",
    "10": "答弁後のフォロー可能性",
    "11": "文章表現・スピーチ技術",
    "12": "行政との協働姿勢・倫理性",
    "13": "将来志向・イノベーション性",
    "14": "政策横断性・全体視点",
    "15": "議員としての成長・継続性"
}

AXIS_LABELS = {
    "A": "核心適合・本質性",
    "B": "明確性・具体性",
    "C": "根拠・裏付け",
    "D": "議会・行政適合性"
}

SCORE_EXPLANATION = {
    5: "完全充足。具体・一義的で実務で修正不要。",
    4: "実務上ほぼ問題なし。軽微な補足不足あり。",
    3: "最低限達成。抽象的で追加説明が必要。",
    2: "不足が明確。実務に結びつかない。",
    1: "形式的・断片的。",
    0: "未達・評価不能。"
}

# ======================================================
# 判定
# ======================================================
def judge_rank(total: int) -> str:
    if total >= 270:
        return "S（模範水準）"
    if total >= 240:
        return "A（非常に優秀）"
    if total >= 210:
        return "B（合格：実務水準）"
    if total >= 180:
        return "C（ボーダー）"
    return "D（不十分）"

# ======================================================
# レーダーチャート
# ======================================================
def show_radar_chart(item_totals):
    labels = [ITEM_NAMES[str(i)] for i in range(1, 16)]
    values = [item_totals[str(i)] for i in range(1, 16)]

    fig = go.Figure()
    fig.add_trace(go.Scatterpolar(
        r=values + [values[0]],
        theta=labels + [labels[0]],
        fill="toself"
    ))

    fig.update_layout(
        polar=dict(radialaxis=dict(range=[0, 20])),
        showlegend=False,
        title="項目別評価分布（20点満点）"
    )

    st.plotly_chart(fig, use_container_width=True)

# ======================================================
# AI採点プロンプト（強化版）
# ======================================================
def build_prompt(text: str, council: str, member: str) -> str:
    council_disp = council.strip() if council.strip() else "未指定"
    member_disp = member.strip() if member.strip() else "未指定"

    return f"""
あなたは地方議会の一般質問を評価する専門家です。

【最重要ルール】
・出力は「JSONオブジェクトのみ」としてください
・前後に文章、説明、コメント、挨拶、注意書きを一切書かないでください
・コードブロック（```）は使用しないでください
・数値はすべて整数（0〜5）としてください
・評価不能な場合は 0 点としてください

【採点方式】
・15項目
・各項目 A〜D の4観点
・各観点 0〜5点
・1項目20点、合計300点
・3点は最低限の到達
・5点は例外的（非常に明確な場合のみ）
・迷った場合は必ず低い点を付けてください

【議会名】
{council_disp}

【議員名】
{member_disp}

【一般質問原稿】
{text}

【出力形式（この構造を厳守）】
{{
  "scores": {{
    "1": {{"A":0,"B":0,"C":0,"D":0}},
    "2": {{"A":0,"B":0,"C":0,"D":0}},
    "3": {{"A":0,"B":0,"C":0,"D":0}},
    "4": {{"A":0,"B":0,"C":0,"D":0}},
    "5": {{"A":0,"B":0,"C":0,"D":0}},
    "6": {{"A":0,"B":0,"C":0,"D":0}},
    "7": {{"A":0,"B":0,"C":0,"D":0}},
    "8": {{"A":0,"B":0,"C":0,"D":0}},
    "9": {{"A":0,"B":0,"C":0,"D":0}},
    "10": {{"A":0,"B":0,"C":0,"D":0}},
    "11": {{"A":0,"B":0,"C":0,"D":0}},
    "12": {{"A":0,"B":0,"C":0,"D":0}},
    "13": {{"A":0,"B":0,"C":0,"D":0}},
    "14": {{"A":0,"B":0,"C":0,"D":0}},
    "15": {{"A":0,"B":0,"C":0,"D":0}}
  }}
}}
"""

# ======================================================
# UI
# ======================================================
st.title("📘 一般質問 採点AIシステム（300点モデル）")
st.caption(f"API利用状況：{st.session_state.api_calls} / {MAX_CALLS}")

# 議会名・議員名（任意）
c1, c2 = st.columns(2)
with c1:
    council_name = st.text_input("🏛 議会名（任意）", placeholder="〇〇市議会")
with c2:
    member_name = st.text_input("🧑‍💼 議員名（任意）", placeholder="山田 太郎")

# 原稿入力（必須）
question_text = st.text_area(
    "▼ 一般質問の原稿を貼り付けてください（必須）",
    height=280
)

# ======================================================
# 採点実行
# ======================================================
if st.button("🚀 AIで自動採点する"):
    check_api_limit(calls=1)

    if not question_text.strip():
        st.error("一般質問原稿が入力されていません。")
        st.stop()

    with st.spinner("AIが採点中…"):
        response = client.chat.completions.create(
            model="gpt-4o",
            messages=[{
                "role": "user",
                "content": build_prompt(
                    question_text,
                    council_name,
                    member_name
                )
            }]
        )
        st.session_state.api_calls += 1

    # ==================================================
    # JSON抽出（耐性あり）
    # ==================================================
    raw = response.choices[0].message.content
    start = raw.find("{")
    end = raw.rfind("}") + 1

    if start == -1 or end == -1:
        st.error("AIの出力にJSONが含まれていません。")
        st.code(raw)
        st.stop()

    json_text = raw[start:end]

    try:
        data = json.loads(json_text)
    except json.JSONDecodeError:
        st.error("AIの出力がJSONとして解析できませんでした。")
        st.code(json_text)
        st.stop()

    scores = data["scores"]

    total = 0
    item_totals = {}
    for i in range(1, 16):
        s = scores[str(i)]
        subtotal = s["A"] + s["B"] + s["C"] + s["D"]
        item_totals[str(i)] = subtotal
        total += subtotal

    # ==================================================
    # 評価対象表示
    # ==================================================
    st.markdown(
        f"""
### 🏛 評価対象
- **議会名**：{council_name if council_name.strip() else "（未入力）"}
- **議員名**：{member_name if member_name.strip() else "（未入力）"}
"""
    )

    # ==================================================
    # 判定表示
    # ==================================================
    if total >= 210:
        st.success(f"🟢 合格：{total} / 300 点（{judge_rank(total)}）")
    elif total >= 180:
        st.warning(f"🟡 ボーダー：{total} / 300 点（{judge_rank(total)}）")
    else:
        st.error(f"🔴 不合格：{total} / 300 点（{judge_rank(total)}）")

    # ==================================================
    # 項目別詳細
    # ==================================================
    for i in range(1, 16):
        with st.expander(f"{i}. {ITEM_NAMES[str(i)]}（{item_totals[str(i)]} / 20点）"):
            for k in ["A", "B", "C", "D"]:
                p = scores[str(i)][k]
                st.markdown(
                    f"**{AXIS_LABELS[k]}：{p}点**｜{SCORE_EXPLANATION[p]}"
                )

    # ==================================================
    # レーダーチャート
    # ==================================================
    st.subheader("📈 レーダーチャート")
    show_radar_chart(item_totals)
